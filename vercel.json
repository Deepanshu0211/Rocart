{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/server.js" },
    { "source": "/(.*)", "destination": "/" }
  ],
  "functions": {
    "api/server.js": {
      "memory": 128,
      "maxDuration": 10
    }
  },
  "buildCommand": "npm install --legacy-peer-deps && npm run build"
}
